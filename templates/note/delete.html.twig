{% include 'default/_form.html.twig' with {
    page_title: 'title_note_delete',
    page_title_param: {'%id%': note.id},
    attr: {
        method: 'DELETE',
        action: url('note_delete', {'id': note.id}),
    },
    form: form,
    submit_label: 'action_delete'|trans,
    back_to_list_path: 'note_index',
} %}